{
  "name": "server",
  "version": "1.0.0",
  "description": "Express API and KNEX PostgreSql database migrations, seeds, and environments.",
  "main": "api.js",
  "scripts": {
    "test": "npm run test-build && jest --detectOpenHandles",
    "build": "npx knex migrate:rollback | npx knex migrate:latest",
    "test-build": "npx knex migrate:rollback --env test && npx knex migrate:latest --env test && npx knex seed:run --env test",
    "start": "nodemon app.js"
  },
  "keywords": [
    "express",
    "api",
    "knex",
    "postresql",
    "sql",
    "database",
    "migration",
    "seed"
  ],
  "author": "yepitsaname",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "jose": "^6.1.0",
    "knex": "^3.1.0",
    "nodemon": "^3.1.10",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@faker-js/faker": "^10.0.0",
    "jest": "^30.1.3",
    "supertest": "^7.1.4"
  }
}
